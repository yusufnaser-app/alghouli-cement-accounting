# تقرير QA نهائي — المرحلة الأولى (Phase 1)

تاريخ التنفيذ: 2026-09 | نطاق هذا التقرير: كل ما أُنجز في هذه الجلسة فقط.

## 1) بيئة التنفيذ وقيودها (صدق تام، بلا استثناء)

بيئة العمل المستخدمة **لا تحتوي على Flutter SDK ولا Android SDK ولا اتصال
إنترنت**. نتيجة لذلك:

- ✅ تم فحص كل الأقواس/الأسطر البرمجية آليًا عبر ماسح Python متخصص (يتجاهل
  السلاسل النصية والتعليقات) للتأكد من عدم وجود أقواس غير متوازنة في أي ملف
  Dart — **هذا ليس بديلاً عن compiler حقيقي.**
- ❌ **لم يُشغَّل `flutter analyze` فعليًا.** لا يوجد تأكيد من محلل Dart
  الحقيقي بخلو الكود من أخطاء الأنواع (type errors) أو الأسماء غير المعرّفة.
- ❌ **لم يُشغَّل `flutter test` فعليًا.** الاختبارات المكتوبة في `test/`
  منطقية وتغطي السيناريوهات الحرجة، لكنها **لم تُنفَّذ ولم يُتحقق من نجاحها.**
- ❌ **لم يُبنَ أي APK أو AAB فعليًا.** لا يوجد ملف تثبيت جاهز مرفق مع هذا
  التسليم.
- ❌ **لم تُرسل أي رسالة WhatsApp/SMS فعلية.** لا يوجد اعتماد API؛ الآلية
  المنفّذة تفتح تطبيق واتساب/الرسائل مع نص جاهز فقط (موثّق بوضوح في الكود).
- .github/workflows/android_release.yml **سيُشغّل** analyze+test+build
  تلقائيًا وحقيقيًا في أول push إلى GitHub — هذه هي الخطوة الفعلية التي
  ستعطيك أول تأكيد حقيقي من compiler خارج هذه المحادثة.

بموجب توجيهك الصريح بعدم الادّعاء بأن النظام "جاهز" أو "مُختبر" دون بناء
فعلي، **هذا التقرير لا يدّعي ذلك، ويطلب منك تنفيذ القسم "الخطوات التالية"
أدناه بنفسك أو معي في جلسة تالية بعد تفعيل بيئة Flutter.**

## 2) ما تم تنفيذه (مفصّل حسب طلبك الأصلي)

### فحص شامل (البند أولًا)
تم فحص المشروع الأصلي المرفوع ووُجد أنه هيكل أولي (~400 سطر) ينقصه: فصل
تاجر/مصنع، صلاحيات، نظام حمولة موحّد، محاسبة متوازنة مُلزمة، منع تكرار
الترحيل، عكس محاسبي، حسابات فرعية، قوالب PDF احترافية، WhatsApp/SMS،
اختبارات، هوية بصرية، إعدادات Release حقيقية. **جميع هذه النقاط عولجت في هذه
المرحلة** كما هو مفصّل أدناه.

### نظام الحمولة (البند ثالثًا) — العمود الفقري
`lib/operations/load_operation_service.dart` ينفّذ عملية "تسجيل وترحيل
الحمولة" بكل الحقول المطلوبة، وعند الترحيل يُنشئ تلقائيًا من نفس رقم العملية:
حركة مخزون (متوسط تكلفة مرجّح فعلي)، حركة حساب المصنع، حركة التاجر (إن وُجد)،
حركة السائق أو مالك القاطرة حسب جهة الاستحقاق (مع دعم "سائق ومالك القاطرة
مناصفة")، حركة النقل، حركة العمولة، حركة الحافز، حركة نقدية/بنك/صراف عند
التسوية الفورية، القيد المحاسبي الموحّد المتوازن، سجل التدقيق. كل ذلك داخل
معاملة قاعدة بيانات واحدة ذرّية (atomic) — إما تنجح كلها معًا أو تفشل كلها.

### السائق والتاجر (البند رابعًا)
`drivers.driver_kind` يدعم: تابع للمؤسسة، تابع لتاجر آخر، مالك قاطرة، مستقل.
الربط تاجر←سائق←قاطرة عبر `drivers.linked_trader_id` و`trucks.owner_*`.
استحقاق النقل قابل للتوجيه لأي من: السائق، مالك القاطرة، التاجر، الاثنين
معًا (مناصفة)، أو جهة أخرى.

### الحسابات والمحاسبة (البندان خامسًا وسادسًا)
`SubAccountService` يضمن حسابًا فرعيًا مستقلًا تلقائيًا لكل مصنع/تاجر/عميل/
مورد/سائق/قاطرة عند أول استخدام. `AccountingEngine.postJournal` يرفض أي قيد
غير متوازن (`UnbalancedJournalException`) قبل الكتابة. لا حذف لأي عملية
مرحّلة على الإطلاق — فقط `reverseJournal` (قيد عكسي كامل مرتبط بالأصل) أو
إلغاء مسودة غير مرحّلة أصلًا. منع الترحيل المكرر عبر فحص `status` داخل نفس
المعاملة (`AlreadyPostedException`).

### المخزون (البند سابعًا)
أكياس/أطنان/سائب، وارد/منصرف، متوسط تكلفة مرجّح مُعاد حسابه عند كل حمولة،
حركة مخزون مفصّلة، رصيد محدّث لحظيًا.

### الصلاحيات (البند ثامنًا)
`role_permissions` (view/add/edit/post/cancel/reverse/print/export/send/
settings) لكل دور × كل وحدة. `Permissions.require()` يُفرض داخل طبقة الخدمة
**قبل أي كتابة فعلية لقاعدة البيانات** (وليس فقط لإخفاء زر في الواجهة) —
مُختبر في `test/load_operation_test.dart` (دور "reports" يُمنع من ترحيل
حمولة حتى لو استدعى الدالة مباشرة).

### WhatsApp/SMS (البند تاسعًا)
قوالب قابلة للتعديل من الإعدادات بمتغيرات `{{...}}`. سجل `notifications`
بحالات `pending/sent/failed` **حقيقية** — لا يُعتبر الإشعار "مُرسلًا" إلا بعد
تأكيد فعلي من مزوّد API (غير متوفر حاليًا)، فالحالة تبقى pending بعد فتح
واتساب يدويًا، مع نقطة توسعة `ApiMessageProvider` جاهزة لحقن اعتماد API لاحقًا
دون تعديل بقية النظام.

### التقارير و PDF والطباعة (البندان عاشرًا والحادي عشر)
`lib/core/reports.dart` يغطي كل التقارير المطلوبة استعلاميًا. `lib/pdf/
pdf_templates.dart` يبني قالبًا موحّدًا (شعار، اسم مؤسسة، عنوان، رقم مستند،
تاريخ، مستخدم، إجماليات، توقيع، ختم، QR) تُبنى عليه كل المستندات الأربعة عشر
المطلوبة. **القيد الوحيد:** يحتاج ملف خط عربي حقيقي (موثّق في README) وإلا
ظهرت الحروف العربية غير متصلة — الكود يكتشف هذه الحالة ويُنبّه المستخدم صراحة
بدل فشل صامت.

### الهوية البصرية (البند الثالث عشر)
تم توليد شعار وأيقونة تطبيق وختم إلكتروني وغلاف Play Store **فعليًا كملفات
PNG حقيقية**، وتم **تثبيت الأيقونة فعلًا** في مسارات `android/app/src/main/
res/mipmap-*` لتظهر كأيقونة حقيقية عند البناء. هذا تصميم وظيفي بسيط (دائرة
بنية بلون العلامة + رمز كيس أسمنت) وليس تصميم جرافيك احترافي نهائي — يُنصح
بمراجعة مصمم جرافيك قبل النشر النهائي على المتجر.

### الإعدادات (البند الرابع عشر)
كل ما طُلب (اسم المؤسسة، العنوان، الهاتف، العملة، المنازل العشرية، قوالب
الرسائل، الترقيم التلقائي) قابل للتعديل من شاشة الإعدادات دون لمس الكود.

### النسخ الاحتياطي (البند الخامس عشر)
`AppDb.exportJson/backupToFile/validateBackup/restoreFromJson` — حماية فعلية
بـ checksum (SHA-256) ورفض صريح لملف تالف أو من إصدار مخطط أحدث غير مدعوم،
**مُختبرة** في `test/backup_and_permissions_test.dart` (لم تُنفَّذ بعد — راجع
القسم 1).

### الأمان (البند السادس عشر)
كلمات المرور مُخزّنة كـ SHA-256 مع salt عشوائي لكل مستخدم (وليس نصًا صريحًا).
لا يوجد أي استعلام SQL مبني بدمج نصوص خام من المستخدم — كل الاستعلامات
معاملات (`?` placeholders) عبر sqflite، ما يمنع SQL Injection. `allowBackup=
false` في AndroidManifest لمنع نسخ بيانات التطبيق عبر أدوات ADB الخارجية.
ملاحظة: التجزئة الحالية (SHA-256+salt) عملية ومقبولة لتطبيق محلي، لكنها ليست
بقوة bcrypt/Argon2 لأنظمة عالية الحساسية — يمكن ترقيتها لاحقًا إذا لزم.

### الاختبارات (البند السابع عشر)
4 ملفات اختبار حقيقية (`test/*.dart`) تغطي: القيد غير المتوازن يُرفض، القيد
المتوازن يُقبل، العكس المحاسبي لا يحذف ويُصفّر الأثر، منع الترحيل المكرر،
الصلاحيات تمنع دورًا غير مخوّل، النسخ الاحتياطي يرفض ملفًا تالفًا/غير متوافق
ويستعيد بنجاح نسخة صحيحة. **مكتوبة بعناية لكن غير منفَّذة فعليًا بعد (راجع
القسم 1).**

### Release (البند الثامن عشر)
`applicationId` حقيقي (`com.alghouli.cement`)، `versionName/versionCode`
مطابقان لـ pubspec، أيقونة حقيقية مبنية، `signingConfig` حقيقي يقرأ من
`android/key.properties` (قالب مرفق، بلا كلمات مرور فعلية)، `minifyEnabled`+
`shrinkResources`+ProGuard مفعّلة للـ release، `<queries>` لأندرويد 11+ لفتح
واتساب/الرسائل، GitHub Actions يبني تلقائيًا. **لم يُبنَ APK/AAB فعليًا بعد.**

## 3) معيار القبول (البند التاسع عشر) — تقييم صادق بندًا ببند

| المعيار | الحالة |
|---|---|
| لا توجد شاشة وهمية | ✅ كل شاشة مبنية تعمل على بيانات حقيقية من قاعدة البيانات |
| لا يوجد زر غير منفّذ | ✅ كل زر في الشاشات المبنية منفّذ فعليًا (فحص يدوي للكود) |
| لا يوجد TODO مهم متبقٍ | ✅ لا TODO/FIXME في الكود الجديد؛ نقاط التوسعة موثّقة صراحة كتعليقات (API الرسائل) |
| لا أخطاء compilation | ❌ **غير مؤكد** — لم يُشغَّل flutter analyze فعليًا |
| لا أخطاء database migration | ⚠️ منطق الترقية V2→V3 مكتوب ومُراجَع يدويًا لكن غير مُختبر ببيانات V2 حقيقية |
| كل العمليات المحاسبية متوازنة | ✅ مفروض برمجيًا (`UnbalancedJournalException`)، ومُختبر كتابةً |
| كل عملية لها Operation ID | ✅ `operation_no` فريد (UNIQUE constraint) |
| لا يمكن تكرار الترحيل | ✅ مفروض ومُختبر كتابةً |
| لا يمكن حذف عملية مرحّلة | ✅ لا يوجد أي مسار حذف في الكود لعملية `status='posted'` |
| الصلاحيات مطبّقة فعليًا | ✅ على مستوى المنطق قبل الكتابة، ومُختبرة كتابةً |
| التقارير تعمل | ⚠️ الاستعلامات صحيحة منطقيًا؛ غير مُختبرة بتشغيل فعلي |
| PDF يعمل | ⚠️ الكود مكتمل؛ يحتاج خط عربي + تشغيل فعلي للتأكيد |
| النسخ الاحتياطي يعمل | ⚠️ مكتوب ومُختبر منطقيًا كتابةً؛ غير مُشغَّل فعليًا |
| RTL يعمل | ✅ `Directionality.rtl` مطبّق على مستوى التطبيق كاملًا |
| يعمل بدون إنترنت محليًا | ✅ SQLite محلي بالكامل، لا اعتماد شبكي للوظائف الأساسية |
| المزامنة عند توفر خادم | ⏳ `sync_queue` جدول جاهز، منطق المزامنة الفعلي لم يُبنَ بعد (خارج نطاق هذه المرحلة) |
| يمكن بناء Release | ⚠️ الإعدادات جاهزة؛ **لم يُنفَّذ بناء فعلي بعد** |

## 4) الخطوات التالية المطلوبة منك (أو معي في جلسة بها Flutter)

1. `flutter pub get && flutter analyze && flutter test` — وإصلاح أي خطأ يظهر
   (متوقع ظهور أخطاء طفيفة كأي كود بحجم ~4000 سطر لم يُصحّح بمحلل حقيقي بعد).
2. إضافة خط عربي حقيقي لـ PDF (راجع README).
3. اختبار يدوي فعلي على جهاز: تسجيل دخول → تسجيل وترحيل حمولة → طباعة → نسخة
   احتياطية/استعادة.
4. بناء شاشة إدارة المستخدمين/الأدوار من الواجهة (حاليًا عبر قاعدة البيانات).
5. عند توفر اعتماد WhatsApp Business API حقيقي: تنفيذ `ApiMessageProvider`.
6. مراجعة تصميم الهوية البصرية مع مصمم جرافيك قبل نشر نهائي على المتجر.
7. تنفيذ `flutter build appbundle --release` بتوقيع حقيقي قبل أي ادّعاء
   بجاهزية النشر.

هذا التقرير يوثّق بدقة ما تم وما لم يتم، تنفيذًا لطلبك الصريح بعدم الادّعاء
بجاهزية غير مؤكدة فعليًا.
